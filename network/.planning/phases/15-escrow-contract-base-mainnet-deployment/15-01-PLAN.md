---
phase: 15-escrow-contract-base-mainnet-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/AgentEscrow.sol
  - scripts/deploy-escrow.ts
  - src/lib/chain/escrow.ts
  - src/lib/chain/abi/AgentEscrow.json
  - package.json
autonomous: true
requirements: [ESC-01, ESC-05]

must_haves:
  truths:
    - "Deploy script compiles AgentEscrow.sol using solc-js npm package (not system solc binary)"
    - "Deploy script targets Base mainnet (not baseSepolia) with correct USDC and Treasury addresses"
    - "escrow.ts TypeScript client uses Base mainnet chain, not baseSepolia"
    - "Event parsing in escrow.ts uses decodeEventLog pattern (not fragile logs[0].topics[1])"
    - "ABI JSON file is generated from compilation and saved to src/lib/chain/abi/"
  artifacts:
    - path: "scripts/deploy-escrow.ts"
      provides: "Solidity compilation via solc-js + Base mainnet deployment"
      contains: "solc.compile"
    - path: "src/lib/chain/escrow.ts"
      provides: "TypeScript escrow client targeting Base mainnet"
      contains: "from 'viem/chains'"
    - path: "src/lib/chain/abi/AgentEscrow.json"
      provides: "Generated ABI from solc compilation"
  key_links:
    - from: "scripts/deploy-escrow.ts"
      to: "contracts/AgentEscrow.sol"
      via: "solc-js compilation with import callback for @openzeppelin"
      pattern: "findImports|import.*callback"
    - from: "src/lib/chain/escrow.ts"
      to: "src/lib/chain/abi/AgentEscrow.json"
      via: "ABI import for contract interaction"
      pattern: "import.*AgentEscrow"
    - from: "src/lib/chain/escrow.ts"
      to: "viem decodeEventLog"
      via: "Event parsing in createJob"
      pattern: "decodeEventLog"
---

<objective>
Install dependencies (@openzeppelin/contracts, solc), update the deploy script to use solc-js with import callbacks (no system solc binary), switch all chain references from baseSepolia to Base mainnet, fix event parsing in escrow.ts to use decodeEventLog, and generate the ABI JSON file.

Purpose: Prepare the contract compilation pipeline and TypeScript client for Base mainnet deployment. The deploy script must be runnable with `npx tsx scripts/deploy-escrow.ts` and the escrow.ts client must work against the deployed mainnet contract.

Output: Updated deploy script, updated escrow.ts client, generated ABI file, installed dependencies.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-escrow-contract-base-mainnet-deployment/15-RESEARCH.md
@contracts/AgentEscrow.sol
@scripts/deploy-escrow.ts
@src/lib/chain/escrow.ts
@src/lib/chain/erc8004.ts
@src/lib/chain/usdc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and update deploy script to use solc-js + Base mainnet</name>
  <files>package.json, scripts/deploy-escrow.ts, src/lib/chain/abi/AgentEscrow.json</files>
  <action>
1. Install dependencies:
   ```
   npm install @openzeppelin/contracts solc
   ```

2. Rewrite `scripts/deploy-escrow.ts` to:
   - Remove the `execSync` system solc call entirely
   - Import `solc` from the npm package (`import solc from 'solc'`)
   - Use `readFileSync` to read `contracts/AgentEscrow.sol`
   - Create a `findImports(importPath: string)` callback that resolves paths from `node_modules/` (e.g., `@openzeppelin/contracts/token/ERC20/IERC20.sol` -> `node_modules/@openzeppelin/contracts/token/ERC20/IERC20.sol`)
   - Call `solc.compile(JSON.stringify(input), { import: findImports })` with standard JSON input including optimizer (enabled: true, runs: 200)
   - Extract ABI and bytecode from compilation output
   - Save ABI to `src/lib/chain/abi/AgentEscrow.json` (mkdir -p the directory)
   - Switch chain from `baseSepolia` to `base` (import from `viem/chains`)
   - Update USDC address to `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` (Base mainnet)
   - Keep TREASURY_ADDRESS default as `0x0eEf9b6C1f35266A2440E0263C5B89AcaDd12d72`
   - Update BaseScan URL from `sepolia.basescan.org` to `basescan.org`
   - Remove the temp `solc-input.json` file write (no longer needed with solc-js)
   - Keep DEPLOYER_KEY from env var `ESCROW_DEPLOYER_PRIVATE_KEY`
   - Update header comment: prerequisites say `npm install @openzeppelin/contracts solc` not `npm install -g solc`

3. Run the compilation step only (without deployment) to verify it works and generate the ABI file:
   - Create a small test: read the contract, compile with solc-js, write ABI to `src/lib/chain/abi/AgentEscrow.json`
   - The deploy script itself should do this as its first step, so just verify the compile portion works by examining the script logic

Note: Do NOT actually deploy the contract (that requires funded deployer wallet). Just ensure the script compiles correctly.
  </action>
  <verify>
- `npm ls @openzeppelin/contracts solc` shows both installed
- `npx tsx -e "import solc from 'solc'; console.log('solc version:', solc.version())"` prints a version
- Run the compilation portion: create a small script or modify deploy-escrow.ts to have a `--compile-only` flag, or just verify the script has no TypeScript errors: `npx tsc --noEmit scripts/deploy-escrow.ts` (or verify no syntax errors with `npx tsx --eval "import('./scripts/deploy-escrow.ts')"` -- this may fail at runtime due to missing DEPLOYER_KEY but should not have import/syntax errors)
  </verify>
  <done>
- @openzeppelin/contracts and solc are in package.json dependencies
- deploy-escrow.ts uses solc npm package with findImports callback (no system solc binary)
- deploy-escrow.ts targets Base mainnet with correct USDC address and basescan.org URL
- ABI directory exists at src/lib/chain/abi/
  </done>
</task>

<task type="auto">
  <name>Task 2: Update escrow.ts client for Base mainnet and fix event parsing</name>
  <files>src/lib/chain/escrow.ts</files>
  <action>
Update `src/lib/chain/escrow.ts` with these changes:

1. **Chain: baseSepolia -> base**
   - Change `import { baseSepolia } from 'viem/chains'` to `import { base } from 'viem/chains'`
   - Update `getPublicClient()` to use `chain: base`
   - Update `getWalletClient()` to use `chain: base`

2. **Import ABI from generated file instead of inline**
   - Import the ABI from `./abi/AgentEscrow.json` (generated by deploy script in Task 1)
   - Remove the inline `escrowAbi` array and use the imported ABI
   - Keep the `approveAbi` inline (it's for USDC, not the escrow contract)
   - If the ABI JSON file doesn't exist yet (deploy hasn't run), keep the inline ABI as fallback but add a comment noting it should match the generated file

3. **Fix createJob event parsing** (critical bug fix)
   - Import `decodeEventLog` from viem
   - Replace the fragile `receipt.logs[0]?.topics[1]` parsing with the `decodeEventLog` iteration pattern from `erc8004.ts`
   - Define a `jobCreatedEventAbi` constant with the JobCreated event signature:
     ```
     { name: 'JobCreated', type: 'event', inputs: [
       { name: 'jobId', type: 'uint256', indexed: true },
       { name: 'client', type: 'address', indexed: true },
       { name: 'agent', type: 'address', indexed: true },
       { name: 'amount', type: 'uint256', indexed: false },
     ]}
     ```
   - Iterate `receipt.logs`, try `decodeEventLog` on each, break when `eventName === 'JobCreated'`
   - Throw an error if JobCreated event not found (same pattern as erc8004.ts line 126)

4. **Export the escrowAbi and ESCROW_ADDRESS** for use by API routes
   - Export `escrowAbi` (or the imported ABI)
   - Export `ESCROW_ADDRESS`

5. **Keep all existing function signatures** (approveEscrowSpend, createJob, releaseJob, disputeJob, resolveDispute, refundJob, getJob) -- only change internals.
  </action>
  <verify>
- `grep -c "baseSepolia" src/lib/chain/escrow.ts` returns 0 (no testnet references)
- `grep -c "base" src/lib/chain/escrow.ts` shows base chain import
- `grep -c "decodeEventLog" src/lib/chain/escrow.ts` returns at least 1
- `grep -c "logs\[0\]" src/lib/chain/escrow.ts` returns 0 (fragile parsing removed)
- `npx tsc --noEmit` passes (or at least escrow.ts has no type errors)
  </verify>
  <done>
- escrow.ts uses `base` chain (not baseSepolia) for all clients
- createJob uses decodeEventLog iteration pattern (matching erc8004.ts)
- escrowAbi and ESCROW_ADDRESS are exported
- All existing function signatures preserved
  </done>
</task>

</tasks>

<verification>
- No references to `baseSepolia` remain in escrow.ts or deploy-escrow.ts
- No reference to system `solc` binary in deploy-escrow.ts (no `execSync('solc ...`)
- `@openzeppelin/contracts` and `solc` in package.json
- deploy-escrow.ts compiles without TypeScript errors
- escrow.ts compiles without TypeScript errors
</verification>

<success_criteria>
- Deploy script ready to run with `npx tsx scripts/deploy-escrow.ts` against Base mainnet (just needs funded deployer wallet)
- escrow.ts client targets Base mainnet with robust event parsing
- All dependencies installed
</success_criteria>

<output>
After completion, create `.planning/phases/15-escrow-contract-base-mainnet-deployment/15-01-SUMMARY.md`
</output>
