---
phase: 06-nft-minting
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/feed/post-card.tsx
  - src/components/profile/nft-portfolio.tsx
  - src/app/agent/[id]/page.tsx
autonomous: true
requirements: [NFT-03, NFT-04]

must_haves:
  truths:
    - "Minted posts display a clickable NFT badge with BaseScan link"
    - "Agent profile portfolio tab shows all minted NFTs for that agent"
    - "Portfolio tab queries posts with nft_contract IS NOT NULL"
  artifacts:
    - path: "src/components/feed/post-card.tsx"
      provides: "Enhanced NFT badge with BaseScan link and token ID"
      contains: "sepolia.basescan.org/token"
    - path: "src/components/profile/nft-portfolio.tsx"
      provides: "NFT portfolio grid component"
      exports: ["NFTPortfolio"]
    - path: "src/app/agent/[id]/page.tsx"
      provides: "Portfolio tab wired to NFTPortfolio component"
      contains: "NFTPortfolio"
  key_links:
    - from: "src/app/agent/[id]/page.tsx"
      to: "src/components/profile/nft-portfolio.tsx"
      via: "import NFTPortfolio"
      pattern: "import.*NFTPortfolio"
    - from: "src/components/profile/nft-portfolio.tsx"
      to: "/api/agents or DB query"
      via: "fetch or server query for NFT posts"
      pattern: "nft_contract IS NOT NULL"
---

<objective>
Enhance post cards with clickable NFT badges linking to BaseScan and build the agent profile NFT portfolio tab showing all minted posts.

Purpose: Make minted NFTs visible across the platform -- badges on feed cards and a dedicated portfolio view on agent profiles.
Output: Enhanced post-card.tsx, new nft-portfolio.tsx component, wired portfolio tab
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-nft-minting/06-01-SUMMARY.md
@src/components/feed/post-card.tsx (has NFT badge stub to enhance)
@src/app/agent/[id]/page.tsx (has portfolio tab placeholder)
@src/lib/types.ts (Post type with nft_contract, nft_token_id, filecoin_cid)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance post card NFT badge with BaseScan link</name>
  <files>src/components/feed/post-card.tsx</files>
  <action>
    Replace the existing NFT badge stub (lines 34-37 of post-card.tsx) with a clickable link:

    When `post.nft_contract` is truthy, render an anchor tag:
    - href: `https://sepolia.basescan.org/token/${post.nft_contract}?a=${post.nft_token_id}`
    - target="_blank", rel="noopener noreferrer"
    - Classes: `mt-2 inline-flex items-center gap-1 text-xs px-2 py-1 rounded bg-purple-500/20 text-purple-300 hover:bg-purple-500/30 transition-colors`
    - Text content: "NFT #{post.nft_token_id}" (show token ID, not just "NFT minted")

    This follows the same external link pattern as the "Buy Token" button from phase 04-02 (using <a> tag for external navigation).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - post-card.tsx contains `sepolia.basescan.org/token` href
    - Badge displays token ID number
  </verify>
  <done>
    - Posts with nft_contract show clickable "NFT #X" badge linking to BaseScan token page
    - Posts without nft_contract show no badge (unchanged behavior)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build NFT portfolio tab on agent profile</name>
  <files>src/components/profile/nft-portfolio.tsx, src/app/agent/[id]/page.tsx</files>
  <action>
    1. Create `src/components/profile/nft-portfolio.tsx`:
       - Props: `{ posts: Post[] }` where Post is from `@/lib/types`
       - If posts array is empty, show: "No NFTs minted yet" placeholder text (same style as current portfolio placeholder)
       - Otherwise render a grid of NFT cards (responsive: 1 col mobile, 2 cols md, 3 cols lg)
       - Each card is a glass-card showing:
         - Post content (truncated to 140 chars with ellipsis if longer)
         - "NFT #{post.nft_token_id}" badge (same purple style as post-card badge)
         - Link to BaseScan: `https://sepolia.basescan.org/token/${post.nft_contract}?a=${post.nft_token_id}`
         - If post.filecoin_cid exists, show small "Filecoin" indicator text
         - Post timestamp via timeAgo helper (extract or duplicate the helper from post-card.tsx)
       - This is a client component only if needed for interactivity; prefer server component if possible

    2. Update `src/app/agent/[id]/page.tsx`:
       - Import NFTPortfolio from `@/components/profile/nft-portfolio`
       - Replace the portfolio tab placeholder (line ~207: "NFT portfolio coming soon" div) with `<NFTPortfolio posts={nftPosts} />`
       - Fetch NFT posts: query posts WHERE agent_id = ? AND nft_contract IS NOT NULL, ordered by created_at DESC
       - The query should join agent fields (display_name, avatar_url, service_type) same as the main posts query
       - Pass the fetched nftPosts to the NFTPortfolio component
       - If the profile page is a client component, fetch via an API call or pass data from a parent server component. Match the existing data fetching pattern used for the posts tab.

    Follow existing cyberpunk design patterns: glass-card class, --color-text-primary/secondary/tertiary CSS vars, purple accent for NFT elements (consistent with post-card badge).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `pnpm build` succeeds
    - nft-portfolio.tsx exists and exports NFTPortfolio
    - agent/[id]/page.tsx imports and renders NFTPortfolio in portfolio tab
  </verify>
  <done>
    - Agent profile portfolio tab shows grid of minted NFTs (or empty state)
    - Each NFT card links to BaseScan and shows content preview
    - Portfolio queries only posts with nft_contract IS NOT NULL
    - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with no errors
- `npx tsc --noEmit` passes
- Post card NFT badge is a clickable link to BaseScan (not just a static label)
- Portfolio tab renders NFTPortfolio component (not placeholder text)
- Empty portfolio shows appropriate empty state
</verification>

<success_criteria>
- Minted posts show "NFT #X" badge linking to BaseScan token page (NFT-03)
- Agent profile portfolio tab displays all minted NFTs in a responsive grid (NFT-04)
- No regressions on existing post card or profile page functionality
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-nft-minting/06-02-SUMMARY.md`
</output>
