---
phase: 14-observability-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_agent_events.sql
  - src/lib/types.ts
autonomous: true
requirements: [OBS-04]

must_haves:
  truths:
    - "agent_events table exists in Supabase and accepts INSERT from supabase-logger.ts"
    - "Supabase Realtime can subscribe to agent_events with filter agent_id=eq.{id}"
    - "AgentEvent TypeScript type is importable in Next.js components"
  artifacts:
    - path: "supabase/migrations/005_agent_events.sql"
      provides: "agent_events table with correct columns and indexes"
      contains: "CREATE TABLE IF NOT EXISTS agent_events"
    - path: "src/lib/types.ts"
      provides: "AgentEvent TypeScript type"
      contains: "AgentEvent"
  key_links:
    - from: "agent-server/src/supabase-logger.ts"
      to: "supabase agent_events table"
      via: "supabase.from('agent_events').insert()"
      pattern: "agent_events"
    - from: "src/lib/types.ts"
      to: "agent-server AgentEvent shape"
      via: "matching column names: agent_id, event_type, payload, created_at"
      pattern: "AgentEvent"
---

<objective>
Create the agent_events Supabase table that supabase-logger.ts already writes to, and add the AgentEvent TypeScript type to the Next.js types file.

Purpose: The table must exist before Supabase Realtime can subscribe to it. The type is needed by the dashboard page in Plan 03.
Output: Migration SQL file + AgentEvent type in src/lib/types.ts.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/004_chat_messages.sql
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent_events Supabase migration</name>
  <files>supabase/migrations/005_agent_events.sql</files>
  <action>
Create `supabase/migrations/005_agent_events.sql` following the exact style of 004_chat_messages.sql.

Table definition must match the AgentEvent interface in agent-server/src/supabase-logger.ts exactly:
- `id` TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text
- `agent_id` TEXT NOT NULL REFERENCES agents(id) ON DELETE CASCADE
- `event_type` TEXT NOT NULL CHECK (event_type IN ('turn_start', 'turn_complete', 'tool_call', 'llm_call', 'error'))
- `payload` JSONB NOT NULL DEFAULT '{}'
- `created_at` TIMESTAMPTZ NOT NULL DEFAULT NOW()

Add indexes:
- `CREATE INDEX IF NOT EXISTS idx_agent_events_agent ON agent_events(agent_id, created_at ASC)` — for Realtime filtered subscriptions and history queries

Enable Supabase Realtime on the table by adding this statement after index creation:
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE agent_events;
```

Add COMMENT ON TABLE and COMMENT ON COLUMN for each column (same style as 004).

This table does NOT need to be applied via `supabase db push` locally — the executor should note in their summary that this migration must be applied via the Supabase dashboard SQL Editor (same approach as all prior migrations in this project).
  </action>
  <verify>File exists at supabase/migrations/005_agent_events.sql. Check column names match supabase-logger.ts AgentEvent interface fields (agent_id, event_type, payload, created_at). Confirm ALTER PUBLICATION line is present.</verify>
  <done>005_agent_events.sql written with correct schema, indexes, Realtime publication, and comments. File is ready to apply via Supabase dashboard SQL Editor.</done>
</task>

<task type="auto">
  <name>Task 2: AgentEvent TypeScript type in Next.js</name>
  <files>src/lib/types.ts</files>
  <action>
Append the AgentEvent TypeScript type to the end of `src/lib/types.ts`.

Add after the AgentStatus type (last type in the file):

```typescript
export interface AgentEvent {
  id: string;
  agent_id: string;
  event_type: 'turn_start' | 'turn_complete' | 'tool_call' | 'llm_call' | 'error';
  payload: Record<string, unknown>;
  created_at: string;
}
```

The payload shapes for each event_type (for reference in Plan 03 dashboard UI):
- `llm_call`: `{ model: string, input_tokens: number, output_tokens: number, prompt_preview?: string }`
- `tool_call`: `{ tool_name: string, input: unknown, output?: unknown, duration_ms?: number }`
- `turn_start`: `{ message_preview?: string }`
- `turn_complete`: `{ total_tokens?: number }`
- `error`: `{ message: string }`

Do NOT add payload-specific sub-types to types.ts — the dashboard page will cast payload inline with type assertions as needed. Only the base AgentEvent interface goes here.

Run `npx tsc --noEmit` from the Next.js app directory after the edit to confirm zero TypeScript errors.
  </action>
  <verify>Run `npx tsc --noEmit` — 0 errors. Grep `AgentEvent` in src/lib/types.ts returns the interface.</verify>
  <done>AgentEvent interface exported from src/lib/types.ts. TypeScript build passes.</done>
</task>

</tasks>

<verification>
1. `supabase/migrations/005_agent_events.sql` exists and contains `CREATE TABLE IF NOT EXISTS agent_events`, the 5 columns, indexes, and `ALTER PUBLICATION supabase_realtime ADD TABLE agent_events`
2. `src/lib/types.ts` exports `AgentEvent` with event_type union matching supabase-logger.ts
3. `npx tsc --noEmit` passes with 0 errors
</verification>

<success_criteria>
- agent_events migration file is complete and ready to apply
- AgentEvent type is importable in Next.js components
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-observability-dashboard/14-01-SUMMARY.md`
</output>
