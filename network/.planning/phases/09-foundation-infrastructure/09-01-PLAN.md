---
phase: 09-foundation-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/001_initial.sql
  - scripts/migrate-to-supabase.mjs
  - .env.local
autonomous: false
requirements:
  - DB-01
  - DB-03
user_setup:
  - service: supabase
    why: "Postgres database replacing SQLite — shared by Next.js and NanoClaw VPS"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API Keys → anon/public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API Keys → service_role (secret — never in NEXT_PUBLIC_*)"
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection String → URI (use Supavisor port 6543 for pooled connection)"
    dashboard_config:
      - task: "Create new Supabase project (free tier)"
        location: "https://supabase.com/dashboard → New project"
      - task: "Enable Row Level Security on agent_events table (future-proofing) — leave other tables at default for Phase 9"
        location: "Supabase Dashboard → Table Editor → agent_events → RLS"

must_haves:
  truths:
    - "Supabase Postgres has all 6 tables from the SQLite schema (agents, posts, follows, bounties, services, filecoin_uploads)"
    - "agents table has an owner_wallet column (text, nullable)"
    - "Row counts in Supabase match SQLite after migration script runs"
    - "supabase-js and @supabase/ssr packages are installed"
    - "better-sqlite3 is removed from package.json"
  artifacts:
    - path: "supabase/migrations/001_initial.sql"
      provides: "Complete Postgres schema matching SQLite tables plus owner_wallet"
      contains: "create table agents"
    - path: "scripts/migrate-to-supabase.mjs"
      provides: "One-time data migration from SQLite to Supabase"
      exports: []
  key_links:
    - from: "scripts/migrate-to-supabase.mjs"
      to: "Supabase agents table"
      via: "supabaseAdmin.from('agents').upsert()"
      pattern: "supabaseAdmin\\.from\\('agents'\\)"
---

<objective>
Set up Supabase Postgres as the new database backend: create the schema, migrate existing SQLite data, install Supabase packages, and remove better-sqlite3.

Purpose: SQLite is single-process and cannot be shared with the NanoClaw VPS. Supabase Postgres allows both Railway (Next.js) and the VPS (NanoClaw) to read/write the same data. This is the prerequisite for all other Phase 9 work.
Output: Supabase project with all tables populated from SQLite, packages swapped, env vars documented.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation-infrastructure/09-RESEARCH.md
@src/lib/db.ts
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create Supabase project and provide credentials</name>
  <action>
    Before Claude can create the schema, the user must:
    1. Go to https://supabase.com/dashboard → New project → choose free tier
    2. Note the project URL, anon key, service_role key, and Supavisor pooled connection string (port 6543)
    3. Add these to .env.local:
       NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
       NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
       SUPABASE_SERVICE_ROLE_KEY=eyJ...
       DATABASE_URL=postgresql://postgres.xxxx:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres
    The DATABASE_URL must use port 6543 (Supavisor pooled connection — required for connection pooling with Railway serverless).
  </action>
  <resume-signal>Type "ready" once .env.local has all four Supabase env vars</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages, create Postgres schema, migrate SQLite data</name>
  <files>
    supabase/migrations/001_initial.sql
    scripts/migrate-to-supabase.mjs
    package.json
  </files>
  <action>
    1. Install packages:
       pnpm add @supabase/supabase-js @supabase/ssr siwe iron-session
       pnpm remove better-sqlite3
       pnpm remove -D @types/better-sqlite3

    2. Create supabase/migrations/001_initial.sql with the full Postgres schema from 09-RESEARCH.md Pattern 5. Key differences from SQLite:
       - All tables use text primary keys (matching existing UUIDs/nanoid IDs)
       - agents table gets NEW column: owner_wallet text (nullable — existing agents have no owner)
       - self_verified changes from INTEGER to boolean
       - created_at/updated_at use timestamptz default now()
       - filecoin_uploads table included
       - All indexes from SQLite schema included
       - Add table comment on agents: 'Owner enforcement: application layer (SIWE session) in v2.0. RLS policy upgrade in future phase.'

    3. Create scripts/migrate-to-supabase.mjs — ESM script that:
       - Reads from .data/network.db using better-sqlite3 (it still exists on disk even if removed from package.json — use direct import from the database file if needed, or use a temp install)
       - Actually: read from .data/network.db using the `better-sqlite3` package that may still be installed. If not available, read the SQLite db using the `sqlite3` CLI by running `sqlite3 .data/network.db .dump > /tmp/dump.sql` and parsing the output.
       - Preferred approach: use `sqlite3` CLI to export each table as CSV, then import via Supabase client.
       - The script should:
         a. Connect to Supabase using SUPABASE_SERVICE_ROLE_KEY (bypass RLS for import)
         b. For each table (agents, posts, follows, bounties, services, filecoin_uploads):
            - Export data from SQLite using `execSync("sqlite3 .data/network.db '.mode json' 'select * from TABLE;'")`
            - Parse JSON output
            - Convert self_verified (0/1) to boolean in agents rows
            - Upsert into Supabase using supabaseAdmin.from('TABLE').upsert(rows)
         c. After each table: log count inserted vs count in SQLite
         d. Final output: "Migration complete. Verify row counts above."

    4. Run the schema migration against Supabase:
       - Use the Supabase CLI if available: `npx supabase db push`
       - If not: use the Supabase Dashboard SQL editor to run 001_initial.sql manually (document the command in the script)
       - Preferred: run the SQL via the service role connection using node -e "..." or by piping to psql using DATABASE_URL

    5. Run the migration script:
       node scripts/migrate-to-supabase.mjs

    Do NOT delete src/lib/db.ts yet — that happens in 09-02 when all routes are migrated.
  </action>
  <verify>
    node -e "
      const { createClient } = await import('@supabase/supabase-js');
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
      const client = createClient(url, key);
      const { count } = await client.from('agents').select('*', { count: 'exact', head: true });
      console.log('Agents in Supabase:', count);
    " --input-type=module

    Also verify via Supabase Dashboard → Table Editor → agents table shows rows.
  </verify>
  <done>
    - supabase/migrations/001_initial.sql exists with all 6 tables and owner_wallet column
    - scripts/migrate-to-supabase.mjs exists and runs without errors
    - Row count in Supabase agents table matches .data/network.db agents table
    - package.json has @supabase/supabase-js, @supabase/ssr, siwe, iron-session
    - package.json does NOT have better-sqlite3
  </done>
</task>

</tasks>

<verification>
1. `pnpm list @supabase/supabase-js` — should show ^2.99.3
2. `pnpm list better-sqlite3` — should show nothing
3. Row counts match between SQLite and Supabase for all 6 tables
4. Supabase Dashboard → Table Editor → agents shows owner_wallet column (nullable)
</verification>

<success_criteria>
- Supabase project exists with all 6 tables populated from SQLite data
- Zero data loss: every row in SQLite is in Supabase
- New packages installed: @supabase/supabase-js, @supabase/ssr, siwe, iron-session
- Removed packages: better-sqlite3
- .env.local has all 4 Supabase env vars
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation-infrastructure/09-01-SUMMARY.md`
</output>
