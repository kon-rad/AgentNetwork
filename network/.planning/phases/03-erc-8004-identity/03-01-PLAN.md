---
phase: 03-erc-8004-identity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chain/erc8004.ts
  - src/lib/agent-card.ts
  - src/lib/agent-log.ts
autonomous: true
requirements: [ID-04, ID-05]
user_setup:
  - service: base-sepolia
    why: "ERC-8004 registration requires gas on Base Sepolia"
    env_vars:
      - name: AGENT_PRIVATE_KEY
        source: "Private key for a Base Sepolia funded wallet (hex with 0x prefix). Fund via https://www.alchemy.com/faucets/base-sepolia"

must_haves:
  truths:
    - "buildAgentCard() produces JSON conforming to ERC-8004 registration-v1 schema"
    - "buildAgentLog() produces structured execution log with timestamps and action entries"
    - "registerAgent() calls IdentityRegistry.register() and returns agentId + txHash"
    - "submitFeedback() calls ReputationRegistry.giveFeedback() and returns txHash"
    - "getReputationSummary() reads reputation from ReputationRegistry"
  artifacts:
    - path: "src/lib/chain/erc8004.ts"
      provides: "registerAgent, submitFeedback, getReputationSummary functions"
      exports: ["registerAgent", "submitFeedback", "getReputationSummary"]
    - path: "src/lib/agent-card.ts"
      provides: "buildAgentCard function generating ERC-8004 registration-v1 JSON"
      exports: ["buildAgentCard"]
    - path: "src/lib/agent-log.ts"
      provides: "buildAgentLog, addLogEntry functions for structured execution logs"
      exports: ["buildAgentLog", "addLogEntry"]
  key_links:
    - from: "src/lib/chain/erc8004.ts"
      to: "0x8004A818BFB912233c491871b3d84c89A494BD9e"
      via: "viem writeContract"
      pattern: "writeContract.*register"
    - from: "src/lib/chain/erc8004.ts"
      to: "0x8004B663056A597Dffe9eCcC1965A193B7388713"
      via: "viem writeContract"
      pattern: "writeContract.*giveFeedback"
---

<objective>
Create the ERC-8004 chain interaction module and agent data generators (agent.json card + agent_log.json).

Purpose: Foundation layer that all ERC-8004 API routes and UI components depend on. Mirrors the existing filecoin.ts pattern with server-only viem wallet client.
Output: Three library modules — erc8004.ts (contract calls), agent-card.ts (card generator), agent-log.ts (log generator)
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-erc-8004-identity/3-RESEARCH.md
@src/lib/chain/filecoin.ts
@src/lib/types.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ERC-8004 chain interaction module</name>
  <files>src/lib/chain/erc8004.ts</files>
  <action>
Create src/lib/chain/erc8004.ts following the exact same pattern as src/lib/chain/filecoin.ts (server-only, viem wallet client, singleton-ish pattern).

Must include:
1. `import 'server-only'` at top
2. Constants for IDENTITY_REGISTRY (`0x8004A818BFB912233c491871b3d84c89A494BD9e`) and REPUTATION_REGISTRY (`0x8004B663056A597Dffe9eCcC1965A193B7388713`) on Base Sepolia
3. Minimal ABI fragments (as const) for:
   - IdentityRegistry: `register(string agentURI) -> uint256`, `tokenURI(uint256) -> string`, `ownerOf(uint256) -> address`
   - ReputationRegistry: `giveFeedback(uint256,int128,uint8,string,string,string,string,bytes32)`, `getSummary(uint256,address[],string,string) -> (uint64,int128,uint8)`
   - Also include the Registered event ABI: `event Registered(uint256 indexed agentId, string agentURI, address indexed owner)` — needed for parsing receipts
4. `getWalletClient()` using `AGENT_PRIVATE_KEY` env var, `baseSepolia` chain, `http()` transport
5. `getPublicClient()` with `baseSepolia` chain, `http()` transport
6. `registerAgent(agentURI: string): Promise<{ agentId: bigint; txHash: string }>` — calls writeContract for register, then waitForTransactionReceipt, then uses viem `decodeEventLog` to parse the Registered event from receipt logs (do NOT use log index position — iterate logs and match by event signature). Return agentId and txHash.
7. `submitFeedback(agentId: bigint, value: number, tag1: string, tag2: string): Promise<string>` — calls giveFeedback with valueDecimals=0, empty endpoint/feedbackURI, zero feedbackHash. Returns txHash.
8. `getReputationSummary(agentId: bigint): Promise<{ count: bigint; value: bigint; decimals: number }>` — calls getSummary with empty clientAddresses array, empty tag1/tag2. Returns parsed result.

Use viem imports from `viem`, `viem/accounts`, `viem/chains`. Do NOT import ethers.
  </action>
  <verify>Run `cd /Users/konradgnat/dev/startups/network && npx tsc --noEmit src/lib/chain/erc8004.ts` — no type errors.</verify>
  <done>erc8004.ts exports registerAgent, submitFeedback, getReputationSummary with correct types; uses viem only; server-only guard present</done>
</task>

<task type="auto">
  <name>Task 2: Create agent card and log generators</name>
  <files>src/lib/agent-card.ts, src/lib/agent-log.ts</files>
  <action>
Create two modules:

**src/lib/agent-card.ts:**
- Export `buildAgentCard(agent: Agent): object` that generates JSON conforming to ERC-8004 registration-v1 schema
- Import Agent type from `@/lib/types`
- Schema fields: `type` (fixed string `https://eips.ethereum.org/EIPS/eip-8004#registration-v1`), `name`, `description`, `image`, `services` (array with one entry containing name, endpoint, version "1.0.0", skills parsed from services_offered, domains from service_type), `x402Support: false`, `active: true`, `registrations: []`, `supportedTrust: ['reputation']`
- Endpoint format: `/api/agents/${agent.id}` (relative — actual domain depends on deployment)
- Parse services_offered with `JSON.parse()` fallback to empty array

**src/lib/agent-log.ts:**
- Export interface `AgentLogEntry` with fields: timestamp (ISO string), action (string), status ('success' | 'failure'), details (Record<string, unknown>)
- Export interface `AgentLog` with fields: agentId (string), erc8004Id (number | null), agentName (string), logs (AgentLogEntry[])
- Export `buildAgentLog(agent: Agent): AgentLog` — creates empty log structure with agent info
- Export `addLogEntry(log: AgentLog, entry: Omit<AgentLogEntry, 'timestamp'>): AgentLog` — adds entry with current ISO timestamp, returns new log (immutable)
- Import Agent type from `@/lib/types`
  </action>
  <verify>Run `cd /Users/konradgnat/dev/startups/network && npx tsc --noEmit src/lib/agent-card.ts src/lib/agent-log.ts` — no type errors.</verify>
  <done>buildAgentCard produces valid registration-v1 JSON; buildAgentLog + addLogEntry produce structured logs; both import Agent type correctly</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- All three files exist and export their functions
- erc8004.ts has `import 'server-only'` guard
- No ethers imports anywhere in the new files
</verification>

<success_criteria>
- ERC-8004 contract interaction module ready for API routes to consume
- Agent card generator produces registration-v1 compliant JSON
- Agent log generator creates structured execution logs
- All files type-check cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-erc-8004-identity/03-01-SUMMARY.md`
</output>
