---
phase: 15-chat-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/008_chat_sessions.sql
  - src/lib/types.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-05

must_haves:
  truths:
    - "chat_sessions table exists in Supabase with id, agent_id, title, nanoclaw_session_id, created_at, last_message_at columns"
    - "chat_messages table has a nullable session_id FK column referencing chat_sessions"
    - "ChatSession TypeScript interface exists in types.ts and ChatMessage includes optional session_id"
  artifacts:
    - path: "supabase/migrations/008_chat_sessions.sql"
      provides: "chat_sessions table + session_id column on chat_messages + indexes"
      contains: "CREATE TABLE IF NOT EXISTS chat_sessions"
    - path: "src/lib/types.ts"
      provides: "ChatSession interface and updated ChatMessage"
      exports: ["ChatSession"]
  key_links:
    - from: "supabase/migrations/008_chat_sessions.sql"
      to: "chat_messages table"
      via: "ALTER TABLE ADD COLUMN session_id"
      pattern: "ALTER TABLE chat_messages"
---

<objective>
Create the chat_sessions Supabase migration and add the ChatSession TypeScript type.

Purpose: Establishes the data layer for session-grouped chat history. The chat_sessions table stores one row per conversation, and chat_messages gains a session_id FK for grouping.
Output: Migration SQL file ready to apply via Supabase dashboard, updated types.ts with ChatSession interface.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chat-history/15-RESEARCH.md
@supabase/migrations/004_chat_messages.sql
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat_sessions migration</name>
  <files>supabase/migrations/008_chat_sessions.sql</files>
  <action>
Create `supabase/migrations/008_chat_sessions.sql` with:

1. `chat_sessions` table:
   - `id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text`
   - `agent_id TEXT NOT NULL REFERENCES agents(id) ON DELETE CASCADE`
   - `title TEXT` (nullable, derived from first user message)
   - `nanoclaw_session_id TEXT` (nullable, populated lazily after first NanoClaw turn)
   - `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
   - `last_message_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`

2. Index: `idx_chat_sessions_agent ON chat_sessions(agent_id, last_message_at DESC)`

3. Add nullable `session_id TEXT REFERENCES chat_sessions(id) ON DELETE SET NULL` column to `chat_messages`

4. Index: `idx_chat_messages_session ON chat_messages(session_id, created_at ASC)`

5. Add table and column comments following the convention in existing migrations (005_agent_events.sql pattern).

Do NOT add a backfill step in the migration. Legacy messages will have session_id = NULL and will be handled lazily by the API routes (Plan 02).
  </action>
  <verify>Check SQL syntax: `cat supabase/migrations/008_chat_sessions.sql` shows valid SQL with CREATE TABLE, ALTER TABLE, CREATE INDEX, and COMMENT statements.</verify>
  <done>Migration file exists with chat_sessions table, session_id FK on chat_messages, both indexes, and table comments.</done>
</task>

<task type="auto">
  <name>Task 2: Add ChatSession type and update ChatMessage</name>
  <files>src/lib/types.ts</files>
  <action>
In `src/lib/types.ts`:

1. Add `ChatSession` interface after the existing `ChatMessage` interface:
```typescript
export interface ChatSession {
  id: string;
  agent_id: string;
  title: string | null;
  nanoclaw_session_id: string | null;
  created_at: string;
  last_message_at: string;
}
```

2. Add optional `session_id` field to the existing `ChatMessage` interface:
```typescript
session_id?: string | null;
```

Do NOT modify any other types.
  </action>
  <verify>Run `npx tsc --noEmit 2>&1 | head -20` from the project root to confirm no type errors are introduced.</verify>
  <done>ChatSession interface exists in types.ts. ChatMessage has optional session_id field. No type errors.</done>
</task>

</tasks>

<verification>
- `supabase/migrations/008_chat_sessions.sql` exists and contains valid SQL
- `src/lib/types.ts` exports ChatSession and ChatMessage includes session_id
- `npx tsc --noEmit` passes without errors
</verification>

<success_criteria>
Migration file is ready to apply to Supabase. ChatSession type and updated ChatMessage type compile without errors and match the migration schema.
</success_criteria>

<output>
After completion, create `.planning/phases/15-chat-history/15-01-SUMMARY.md`
</output>
