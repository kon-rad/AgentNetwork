---
phase: 04-clanker-tokens
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/profile/token-info.tsx
  - src/app/agent/[id]/page.tsx
autonomous: true
requirements: [TOK-02, TOK-03]

must_haves:
  truths:
    - "Agent profile page shows token symbol and contract address when token is deployed"
    - "Buy Token button links to Uniswap V4 swap page for the agent's token on Base"
    - "Token contract address links to BaseScan"
    - "Profile page gracefully handles agents without deployed tokens"
  artifacts:
    - path: "src/components/profile/token-info.tsx"
      provides: "Token info display component with Uniswap and BaseScan links"
      min_lines: 25
    - path: "src/app/agent/[id]/page.tsx"
      provides: "Updated agent profile page with TokenInfo component"
      contains: "TokenInfo"
  key_links:
    - from: "src/components/profile/token-info.tsx"
      to: "Uniswap swap URL"
      via: "anchor tag href"
      pattern: "app\\.uniswap\\.org/swap"
    - from: "src/components/profile/token-info.tsx"
      to: "BaseScan token URL"
      via: "anchor tag href"
      pattern: "basescan\\.org/token"
    - from: "src/app/agent/[id]/page.tsx"
      to: "src/components/profile/token-info.tsx"
      via: "import TokenInfo"
      pattern: "import.*TokenInfo.*from.*profile/token-info"
---

<objective>
Add token information display and Uniswap trade link to agent profile pages.

Purpose: Show deployed token details (symbol, contract address, BaseScan link) and provide a "Buy Token" button linking to Uniswap V4 swap — fulfilling the agent token UI requirements.
Output: TokenInfo component integrated into agent profile page
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clanker-tokens/4-RESEARCH.md
@.planning/phases/04-clanker-tokens/04-01-SUMMARY.md
@src/app/agent/[id]/page.tsx
@src/components/profile/erc8004-status.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TokenInfo component</name>
  <files>src/components/profile/token-info.tsx</files>
  <action>
Create `src/components/profile/token-info.tsx` as a client component following the exact same styling patterns as `src/components/profile/erc8004-status.tsx` (glass-card, cyberpunk color tokens).

Props: `{ tokenSymbol: string | null; tokenAddress: string | null }`

**When token is deployed (tokenAddress is not null):**
- Display in a glass-card with rounded-xl p-5 (same as ERC8004Status)
- Show a badge: "ERC-20 Token" in cyan (same pattern as "ERC-8004 Registered" badge)
- Show token symbol: `$SYMBOL` in text-[--color-text-primary]
- Show contract address (truncated: first 6 + last 4 chars) with a link to `https://basescan.org/token/${tokenAddress}` (opens in new tab)
- Show a "Buy on Uniswap" button: styled as primary action button (bg-[--color-cyan]/10, border border-[--color-cyan]/20, text-[--color-cyan], hover:bg-[--color-cyan]/20), links to `https://app.uniswap.org/swap?inputCurrency=ETH&outputCurrency=${tokenAddress}&chain=base` (opens in new tab)
- Show a "View on BaseScan" link below (text-sm, text-[--color-cyan], hover:underline) pointing to `https://basescan.org/token/${tokenAddress}`

**When token is NOT deployed (tokenAddress is null):**
- If tokenSymbol exists: show glass-card with "Token: $SYMBOL" and text "Not yet deployed" in text-[--color-text-tertiary]
- If tokenSymbol is also null: return null (no card rendered)
  </action>
  <verify>
`pnpm build` passes. Component file exists and exports TokenInfo.
  </verify>
  <done>
TokenInfo component renders token details with Uniswap swap link and BaseScan link when deployed, shows "not deployed" state when pending, and returns null for agents without token info.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate TokenInfo into agent profile page</name>
  <files>src/app/agent/[id]/page.tsx</files>
  <action>
Update `src/app/agent/[id]/page.tsx`:

1. Import TokenInfo: `import { TokenInfo } from "@/components/profile/token-info";`

2. Add TokenInfo to the Identity grid section (the `grid grid-cols-1 md:grid-cols-2 gap-4 mb-6` div that already contains ERC8004Status and ReputationCard). Change grid to accommodate 3 cards:
   - Update grid classes to `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6` (3 cols on large screens)
   - Keep ERC8004Status and ReputationCard as they are
   - Add `<TokenInfo tokenSymbol={agent.token_symbol} tokenAddress={agent.token_address} />` as the third card

3. Update the existing "Buy $TOKEN" button in the profile header actions section:
   - When `agent.token_address` exists: change from plain button to an `<a>` tag linking to `https://app.uniswap.org/swap?inputCurrency=ETH&outputCurrency=${agent.token_address}&chain=base` (target="_blank", rel="noopener noreferrer"), keep same styling
   - When `agent.token_address` is null but `agent.token_symbol` exists: keep as disabled button with same visual style but add `opacity-50 cursor-not-allowed`

4. In the stats row, if `agent.token_address` exists, make the `$TOKEN_SYMBOL` text a link to `https://basescan.org/token/${agent.token_address}` with hover:underline.
  </action>
  <verify>
`pnpm build` passes. Visit `/agent/{id}` in browser — TokenInfo card appears in the identity grid. Buy button links to Uniswap when token_address is set.
  </verify>
  <done>
Agent profile page shows TokenInfo card in identity grid. Buy button links to Uniswap V4 swap page when token is deployed. Token symbol in stats row links to BaseScan.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes with no errors
- TokenInfo component follows glass-card styling pattern from erc8004-status.tsx
- Uniswap link format: `https://app.uniswap.org/swap?inputCurrency=ETH&outputCurrency={address}&chain=base`
- BaseScan link format: `https://basescan.org/token/{address}`
- Profile page renders correctly with and without token_address
</verification>

<success_criteria>
- Agent profile shows token symbol, contract address, and trade link when token is deployed
- "Buy Token" button links to correct Uniswap V4 swap URL on Base
- Contract address links to BaseScan token page
- Agents without tokens show graceful empty/pending state
</success_criteria>

<output>
After completion, create `.planning/phases/04-clanker-tokens/04-02-SUMMARY.md`
</output>
