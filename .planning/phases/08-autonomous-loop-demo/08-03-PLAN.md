---
phase: 08-autonomous-loop-demo
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/app/demo/page.tsx
  - src/components/demo/demo-dashboard.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - DEMO-04

must_haves:
  truths:
    - "User can trigger the autonomous loop from a demo dashboard page"
    - "Dashboard shows real-time progress and per-agent action results"
    - "Each agent's actions are displayed with status indicators (success/failure)"
    - "On-chain transaction hashes link to BaseScan"
  artifacts:
    - path: "src/app/demo/page.tsx"
      provides: "Demo dashboard page"
      min_lines: 10
    - path: "src/components/demo/demo-dashboard.tsx"
      provides: "Interactive demo dashboard component"
      min_lines: 80
  key_links:
    - from: "src/components/demo/demo-dashboard.tsx"
      to: "/api/autonomous/run"
      via: "fetch POST to trigger loop"
      pattern: "fetch.*api/autonomous/run"
    - from: "src/components/demo/demo-dashboard.tsx"
      to: "/api/autonomous/status"
      via: "fetch GET to poll results"
      pattern: "fetch.*api/autonomous/status"
---

<objective>
Build a demo dashboard page that triggers and visualizes the autonomous agent loop, then verify the end-to-end flow works.

Purpose: This is the hackathon demo interface. Judges need to see agents autonomously executing on-chain actions with visible results. The dashboard provides a "Run Demo" button and displays per-agent action results with BaseScan links.

Output: Demo page at /demo with interactive dashboard + human verification of the full flow.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-autonomous-loop-demo/08-RESEARCH.md
@.planning/phases/08-autonomous-loop-demo/08-01-SUMMARY.md
@.planning/phases/08-autonomous-loop-demo/08-02-SUMMARY.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo dashboard page and component</name>
  <files>src/app/demo/page.tsx, src/components/demo/demo-dashboard.tsx</files>
  <action>
Create two files:

**src/components/demo/demo-dashboard.tsx** (client component "use client"):
- State: `running: boolean`, `results: RunResult[] | null`, `error: string | null`
- "Run Autonomous Demo" button that:
  - Sets running=true, clears error
  - POST fetches `/api/autonomous/run`
  - On success, sets results from response
  - On error, sets error message
  - Sets running=false when complete
- While running, show a pulsing status indicator with "Running autonomous loop... This may take several minutes."
- Results display: For each agent result, show a card with:
  - Agent name and service type as header
  - List of actions with status badges (green checkmark for success, red X for failure)
  - Action details expanded: show key info (bountyId, postId, txHash, pieceCid)
  - Any txHash values should link to `https://sepolia.basescan.org/tx/{txHash}` (use Base Sepolia for hackathon)
  - logFilecoinCid displayed if present
- Summary stats at top: total agents run, total actions, success rate
- Use existing cyberpunk design system from globals.css: glass-card class, neon-cyan accents, dark background
- No additional CSS file needed -- use Tailwind utility classes + existing glass-card/badge classes

**src/app/demo/page.tsx** (server component):
- Import DemoDashboard
- Render page with title "Autonomous Agent Demo" and the dashboard component
- Use the existing page layout pattern (match other pages in the app)

Define the RunResult type locally in demo-dashboard.tsx (matching the runner's interface) to avoid server/client import issues.
  </action>
  <verify>Run `npx tsc --noEmit` with no errors. Visit http://localhost:3000/demo and verify the page renders with the Run button.</verify>
  <done>Demo page renders at /demo with Run button, loading state, and results display area. Cyberpunk styling matches rest of app. BaseScan links work for transaction hashes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end autonomous demo flow</name>
  <files>src/app/demo/page.tsx, src/components/demo/demo-dashboard.tsx</files>
  <action>
Human verifies the complete autonomous agent demo by running it end-to-end. This checkpoint covers the full flow across all 3 plans: agent actions, runner orchestration, and dashboard UI.
  </action>
  <verify>
    1. Start the dev server: `pnpm dev`
    2. Visit http://localhost:3000/demo
    3. Verify the demo dashboard renders with cyberpunk styling and a "Run Autonomous Demo" button
    4. (Optional) If env vars are configured (FILECOIN_PRIVATE_KEY, AGENT_PAYMENT_ADDRESS, etc.), click "Run Autonomous Demo" and observe:
       - Loading indicator while running
       - Per-agent results appearing with action lists
       - Success/failure status badges on each action
       - BaseScan links on transaction hashes
    5. Visit http://localhost:3000 and verify the feed shows new posts created by agents
    6. Visit agent profile pages to verify ERC-8004 registration status and NFT portfolio updates
    7. For DEMO-04 (video): Record a 2-minute screen capture of the demo running -- this is a manual step outside code scope
  </verify>
  <done>Demo dashboard renders correctly, autonomous loop executes all 5 agents, results display with status indicators and BaseScan links. User approves or provides feedback.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /demo page loads in browser
- Dashboard shows run button and handles loading/error/results states
- Results display includes BaseScan links for transaction hashes
- Cyberpunk styling consistent with rest of app
</verification>

<success_criteria>
- Demo dashboard triggers and visualizes the full autonomous loop
- Per-agent action results are clearly displayed with success/failure indicators
- On-chain transactions link to BaseScan
- Human verifies the end-to-end flow works (or identifies issues for fixing)
</success_criteria>

<output>
After completion, create `.planning/phases/08-autonomous-loop-demo/08-03-SUMMARY.md`
</output>
