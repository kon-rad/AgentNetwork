---
phase: 03-erc-8004-identity
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/profile/erc8004-status.tsx
  - src/components/profile/reputation-card.tsx
  - src/app/agent/[id]/page.tsx
autonomous: true
requirements: [ID-03, ID-06]

must_haves:
  truths:
    - "Agent profile page shows ERC-8004 registration status with BaseScan link when registered"
    - "Agent profile page shows 'Not Registered' state when agent has no erc8004_token_id"
    - "Agent profile page shows reputation feedback count and average rating"
    - "Register button triggers POST /api/agents/{id}/register and updates UI on success"
  artifacts:
    - path: "src/components/profile/erc8004-status.tsx"
      provides: "ERC-8004 registration status badge, BaseScan link, and register button"
      exports: ["ERC8004Status"]
    - path: "src/components/profile/reputation-card.tsx"
      provides: "Reputation summary display with feedback count and value"
      exports: ["ReputationCard"]
    - path: "src/app/agent/[id]/page.tsx"
      provides: "Updated agent profile integrating ERC-8004 status and reputation"
  key_links:
    - from: "src/components/profile/erc8004-status.tsx"
      to: "/api/agents/{id}/register"
      via: "fetch POST on button click"
      pattern: "fetch.*register"
    - from: "src/components/profile/reputation-card.tsx"
      to: "/api/agents/{id}/feedback"
      via: "fetch GET on mount"
      pattern: "fetch.*feedback"
    - from: "src/app/agent/[id]/page.tsx"
      to: "src/components/profile/erc8004-status.tsx"
      via: "component import"
      pattern: "ERC8004Status"
---

<objective>
Add ERC-8004 registration status and reputation display to agent profile pages.

Purpose: Users need to see which agents are registered on-chain, click through to BaseScan to verify, trigger registration for unregistered agents, and view reputation feedback. This is the user-facing layer for all ERC-8004 functionality.
Output: Two new components (erc8004-status, reputation-card) integrated into the existing agent profile page.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-erc-8004-identity/3-RESEARCH.md
@src/app/agent/[id]/page.tsx
@src/lib/types.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ERC-8004 status and reputation components</name>
  <files>src/components/profile/erc8004-status.tsx, src/components/profile/reputation-card.tsx</files>
  <action>
**src/components/profile/erc8004-status.tsx:**
Create a "use client" component `ERC8004Status` that accepts `{ agentId: string; tokenId: string | null }` props.

Two states:
1. **Registered** (tokenId is not null): Show a glass-card with:
   - Cyan "ERC-8004 Registered" badge (use existing cyberpunk palette)
   - "Token ID: #{tokenId}" text
   - "View on BaseScan" link opening `https://sepolia.basescan.org/token/0x8004A818BFB912233c491871b3d84c89A494BD9e?a=${tokenId}` in new tab
   - Use `text-[--color-cyan]` for the badge and link, consistent with existing UI

2. **Not Registered** (tokenId is null): Show a glass-card with:
   - Gray "Not Registered" text
   - "Register Identity" button (cyan border, same style as Follow button in profile page)
   - On click: POST to `/api/agents/${agentId}/register`, show loading state ("Registering..." with disabled button), on success update to registered state (use local useState to track), on error show error message briefly
   - Registration may take 10-30 seconds. Show a pulsing/shimmer effect on the button during the request.

**src/components/profile/reputation-card.tsx:**
Create a "use client" component `ReputationCard` that accepts `{ agentId: string; tokenId: string | null }` props.

- If tokenId is null, render nothing (return null)
- On mount (useEffect), fetch GET `/api/agents/${agentId}/feedback`
- Display in a glass-card:
  - "Reputation" heading
  - Feedback count (from response.count)
  - Average rating display (from response.value / response.count if count > 0, else "No ratings yet")
  - Use `text-[--color-neon-green]` for positive ratings
- Handle loading state with small shimmer placeholder
- Handle fetch errors gracefully (show "Unable to load reputation" text)

Both components should use the existing cyberpunk design system: glass-card class, --color-cyan, --color-neon-green, --color-text-primary/secondary/tertiary CSS variables.
  </action>
  <verify>Run `cd /Users/konradgnat/dev/startups/network && npx tsc --noEmit src/components/profile/erc8004-status.tsx src/components/profile/reputation-card.tsx` — no type errors.</verify>
  <done>ERC8004Status shows registered/unregistered state with BaseScan link and register button; ReputationCard displays on-chain reputation summary; both use cyberpunk design system</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ERC-8004 components into agent profile page</name>
  <files>src/app/agent/[id]/page.tsx</files>
  <action>
Modify the existing agent profile page to include ERC-8004 status and reputation:

1. Import `ERC8004Status` from `@/components/profile/erc8004-status` and `ReputationCard` from `@/components/profile/reputation-card`

2. Add an "Identity" section between the profile header card and the tabs. Layout:
   ```
   <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
     <ERC8004Status agentId={agent.id} tokenId={agent.erc8004_token_id} />
     <ReputationCard agentId={agent.id} tokenId={agent.erc8004_token_id} />
   </div>
   ```

3. Update the existing `erc8004_token_id` display in the wallet info section (line ~120): Keep the existing "ERC-8004 ID: #..." text but also add a small BaseScan link icon/text next to it. If tokenId exists, make it a link to `https://sepolia.basescan.org/token/0x8004A818BFB912233c491871b3d84c89A494BD9e?a=${agent.erc8004_token_id}` with `target="_blank" rel="noopener noreferrer"`.

Do NOT change any other existing functionality. Keep all existing imports, state, tabs, and layout intact. Only ADD the new section and imports.
  </action>
  <verify>Run `cd /Users/konradgnat/dev/startups/network && pnpm build` — build completes without errors.</verify>
  <done>Agent profile page shows ERC-8004 registration status card with BaseScan link; reputation card visible for registered agents; existing profile functionality unchanged</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- Agent profile page renders ERC-8004 status section
- Registered agents show token ID and BaseScan link
- Unregistered agents show "Register Identity" button
- Reputation card fetches and displays feedback data
</verification>

<success_criteria>
- ERC-8004 registration status visible on every agent profile
- BaseScan link works for registered agents (correct URL format)
- Register button triggers registration flow with loading state
- Reputation feedback displayed when available
- All components follow existing cyberpunk design system
</success_criteria>

<output>
After completion, create `.planning/phases/03-erc-8004-identity/03-03-SUMMARY.md`
</output>
