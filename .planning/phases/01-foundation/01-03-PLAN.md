---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/layout.tsx
  - src/components/layout/navbar.tsx
  - src/components/agents/agent-card.tsx
  - src/components/feed/post-card.tsx
  - src/components/bounties/bounty-card.tsx
  - src/components/ui/skeleton.tsx
  - src/app/page.tsx
  - src/app/feed/page.tsx
  - src/app/bounties/page.tsx
  - src/app/agent/[id]/page.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "All pages display cyberpunk glassmorphism aesthetic with electric cyan accents, noise texture, and grid background"
    - "Card components use .glass-card class with hover glow effects"
    - "Loading states show shimmer skeleton effects instead of blank screens"
    - "Layout is responsive on desktop and tablet (grid collapses properly)"
    - "Page load animations (fadeInUp) and card hover transitions are visible"
  artifacts:
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton loader components (AgentCardSkeleton, PostCardSkeleton, BountyCardSkeleton)"
      min_lines: 30
    - path: "src/components/layout/navbar.tsx"
      provides: "Navbar with cyberpunk styling (glassmorphism, cyan border, backdrop blur)"
      contains: "glass"
    - path: "src/components/agents/agent-card.tsx"
      provides: "Agent card with .glass-card class and cyberpunk design tokens"
      contains: "glass-card"
  key_links:
    - from: "src/components/ui/skeleton.tsx"
      to: "src/app/globals.css"
      via: "Uses .shimmer class defined in globals.css"
      pattern: "shimmer"
    - from: "src/app/page.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "Renders skeleton during loading state"
      pattern: "Skeleton"
    - from: "src/components/agents/agent-card.tsx"
      to: "src/app/globals.css"
      via: "Uses .glass-card class for cyberpunk card styling"
      pattern: "glass-card"
---

<objective>
Apply the cyberpunk design system from globals.css to all existing components, add skeleton loading states, fix the font configuration, and ensure responsive layout.

Purpose: The CSS design tokens (glassmorphism, cyan accents, animations) exist in globals.css but components still use generic Tailwind classes (border-zinc-800, bg-zinc-900). This plan bridges that gap for a polished cyberpunk aesthetic.
Output: All components use cyberpunk design tokens; skeleton loaders replace blank loading states; responsive grid layout on all pages.
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/1-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/app/globals.css
@src/components/layout/navbar.tsx
@src/components/agents/agent-card.tsx
@src/components/feed/post-card.tsx
@src/components/bounties/bounty-card.tsx
@src/app/page.tsx
@src/app/feed/page.tsx
@src/app/bounties/page.tsx
@src/app/agent/[id]/page.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton components and fix font configuration</name>
  <files>
    src/components/ui/skeleton.tsx
    src/app/layout.tsx
  </files>
  <action>
1. Create `src/components/ui/skeleton.tsx` with skeleton loaders using the existing `.shimmer` class from globals.css:
   - `AgentCardSkeleton`: Mimics agent-card layout — a glass-card container with a shimmer circle (avatar), shimmer rectangles for name (h-4 w-32), service type (h-3 w-20), bio (h-8 w-full), and stats (h-3 w-24). Use `rounded-xl p-5` to match agent-card padding.
   - `PostCardSkeleton`: Mimics post-card layout — shimmer avatar circle, shimmer name line, shimmer content block (2-3 lines), shimmer engagement counts.
   - `BountyCardSkeleton`: Mimics bounty-card layout — shimmer title bar, shimmer description block, shimmer reward amount, shimmer status badge.
   - `SkeletonGrid`: A wrapper that renders N skeleton cards in a responsive grid. Accept `count` prop (default 6) and `type` prop ("agent" | "post" | "bounty"). Render in a grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` for agents, `flex flex-col gap-4` for posts, `grid grid-cols-1 md:grid-cols-2 gap-4` for bounties.
   - All skeleton items should have the `.glass-card` class for consistent appearance.
   - Add stagger animation classes: each skeleton item gets `animate-fade-in stagger-{n}` for sequential fade-in effect.

2. Fix font configuration in `src/app/layout.tsx`:
   - The globals.css `@theme` block sets `--font-sans: var(--font-syne)` but Syne is never loaded.
   - Import `Syne` from `next/font/google`.
   - Configure: `const syne = Syne({ variable: '--font-syne', subsets: ['latin'] })`.
   - Add `syne.variable` to the `<html>` className alongside the existing geist variables.
   - Keep Geist Mono for `--font-mono` (code/monospace text).
   - Update body className from `bg-zinc-950 text-white` to `bg-[--color-bg-primary] text-[--color-text-primary]` to use the cyberpunk design tokens.
  </action>
  <verify>
Run `grep "shimmer" src/components/ui/skeleton.tsx` — should match (skeleton uses shimmer class).
Run `grep "Syne" src/app/layout.tsx` — should match (font imported).
Run `grep "color-bg-primary" src/app/layout.tsx` — should match (design token used).
Run `pnpm build 2>&1 | tail -20` — build should pass.
  </verify>
  <done>Skeleton components exist for agents, posts, and bounties. Syne font loaded and --font-syne CSS variable populated. Body uses cyberpunk design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Apply cyberpunk design tokens to all components and pages</name>
  <files>
    src/components/layout/navbar.tsx
    src/components/agents/agent-card.tsx
    src/components/feed/post-card.tsx
    src/components/bounties/bounty-card.tsx
    src/app/page.tsx
    src/app/feed/page.tsx
    src/app/bounties/page.tsx
    src/app/agent/[id]/page.tsx
  </files>
  <action>
1. Update `src/components/layout/navbar.tsx`:
   - Replace `border-b border-zinc-800 bg-zinc-950` with `border-b border-[--color-border] bg-[--color-bg-primary]/80 backdrop-blur-md` for glassmorphism navbar.
   - Replace active link colors: `bg-zinc-800 text-white` becomes `bg-[--color-cyan]/10 text-[--color-cyan]`.
   - Replace inactive link colors: `text-zinc-400 hover:text-white hover:bg-zinc-900` becomes `text-[--color-text-secondary] hover:text-[--color-text-primary] hover:bg-white/5`.
   - Add `text-glow-cyan` class to the "NETWORK" logo text for cyberpunk glow effect.

2. Update `src/components/agents/agent-card.tsx`:
   - Replace the outer Link className: remove `block border border-zinc-800 rounded-xl p-5 hover:border-zinc-600 hover:bg-zinc-900/50 transition-all`.
   - Replace with: `glass-card block rounded-xl p-5` — the .glass-card class in globals.css already provides glassmorphism background, cyan border, hover glow, and translateY(-2px) on hover.
   - Replace avatar placeholder colors: `bg-zinc-800 text-zinc-400` becomes `bg-[--color-cyan]/10 text-[--color-cyan]`.
   - Replace text colors: `text-white` becomes `text-[--color-text-primary]`, `text-zinc-400` becomes `text-[--color-text-secondary]`, `text-zinc-500` becomes `text-[--color-text-tertiary]`.
   - Replace the inline SERVICE_COLORS object with badge classes from globals.css: use `badge-{service_type}` classes (e.g., `badge-filmmaker`, `badge-coder`) instead of inline Tailwind color classes. These are already defined in globals.css.
   - Add `animate-fade-in-up` class to the outer element for entrance animation.

3. Update `src/components/feed/post-card.tsx`:
   - Read current file first.
   - Apply same pattern: replace generic zinc colors with cyberpunk design tokens.
   - Use `.glass-card` for the card container.
   - Use `text-[--color-text-primary]`, `text-[--color-text-secondary]`, `text-[--color-text-tertiary]` for text hierarchy.
   - Add `animate-fade-in-up` for entrance animation.

4. Update `src/components/bounties/bounty-card.tsx`:
   - Read current file first.
   - Apply same pattern as agent-card and post-card.
   - Use `.glass-card` for container.
   - Use cyberpunk design tokens for text colors.
   - Status badges: open = cyan, claimed = gold, completed = neon-green (using --color-cyan, --color-gold, --color-neon-green).
   - Add `animate-fade-in-up` for entrance animation.

5. Update page components (`src/app/page.tsx`, `src/app/feed/page.tsx`, `src/app/bounties/page.tsx`, `src/app/agent/[id]/page.tsx`):
   - Read each file first to understand current structure.
   - Replace any `bg-zinc-*` background colors with `bg-[--color-bg-primary]` or remove (body already has the background).
   - Replace text colors with design tokens.
   - Ensure agent grid uses responsive classes: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` (UI-02).
   - Replace "Loading..." text states with the skeleton components from Task 1. Import `SkeletonGrid` or individual skeleton components. Show skeletons while `loading` state is true.
   - Add stagger classes to card lists: each card gets `stagger-{index}` class for sequential animation (cap at stagger-8).
   - For page headings, add `text-glow-cyan` to main titles for the cyberpunk glow effect.
  </action>
  <verify>
Run `grep -r "glass-card" src/components/` — should match in agent-card.tsx, post-card.tsx, bounty-card.tsx.
Run `grep -r "border-zinc-800" src/components/` — should NOT match (old styles removed).
Run `grep -r "Skeleton" src/app/page.tsx src/app/feed/page.tsx src/app/bounties/page.tsx` — should match (skeletons used in loading states).
Run `grep -r "animate-fade-in" src/components/` — should match (entrance animations added).
Run `pnpm build 2>&1 | tail -20` — build should pass.
  </verify>
  <done>All components use cyberpunk design tokens (.glass-card, --color-cyan, text-glow-cyan). Loading states show shimmer skeletons. Pages have responsive grid layouts. Entrance animations and hover transitions are applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of cyberpunk UI and wallet connection</name>
  <files>none</files>
  <action>
Start the dev server with `pnpm dev` and open http://localhost:3000 in the browser. Present the verification checklist to the user and wait for approval.
  </action>
  <verify>User confirms visual and functional checks pass by typing "approved".</verify>
  <done>User has visually confirmed the cyberpunk aesthetic, wallet connection, skeleton loading, responsive layout, and animations all work correctly.</done>
  <what-built>Complete Phase 1 foundation: cyberpunk UI with glassmorphism cards, wallet connection via RainbowKit, ENS name display, skeleton loading states, and responsive layout.</what-built>
  <how-to-verify>
1. Run `pnpm dev` and open http://localhost:3000
2. VISUAL CHECK — Cyberpunk aesthetic:
   - Background should show subtle grid lines and noise texture
   - "NETWORK" logo should have cyan glow effect
   - Navbar should have glassmorphism (semi-transparent with backdrop blur)
   - All cards (agents, bounties, posts) should have glass effect with cyan border on hover
3. WALLET CHECK:
   - Click the Connect button in navbar — RainbowKit modal should appear
   - If you connect a wallet, address or ENS name should show in the button
   - Chain icon should be visible next to the address
4. LOADING CHECK:
   - On first load or refresh, skeleton shimmer cards should appear briefly before content loads
5. RESPONSIVE CHECK:
   - Resize browser to tablet width (~768px) — grid should collapse to 2 columns
   - Resize to mobile width (~375px) — grid should collapse to 1 column
6. ANIMATION CHECK:
   - Cards should fade in with staggered animation on page load
   - Cards should lift slightly and glow on hover
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
1. All components use `.glass-card` class (no more `border-zinc-800`)
2. Skeleton loaders shown during loading states on all pages
3. Syne font loaded and applied as `--font-sans`
4. Body uses `--color-bg-primary` and `--color-text-primary`
5. Responsive grid: 3 cols on lg, 2 on md, 1 on mobile
6. FadeInUp animations on card entrance, hover lift + glow on cards
7. `pnpm build` passes
</verification>

<success_criteria>
- Cyberpunk glassmorphism aesthetic visible on all pages (glass cards, cyan accents, noise texture, grid background)
- Responsive layout works on desktop, tablet, and mobile widths
- Page transitions and card hover animations provide visual polish
- Loading states use shimmer skeleton effects instead of blank screens or "Loading..." text
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
