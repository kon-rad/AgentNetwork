---
phase: 07-self-protocol-zk
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/self/self-qr.tsx
  - src/app/verify/[agentId]/page.tsx
autonomous: true
requirements:
  - SELF-01
  - SELF-03

must_haves:
  truths:
    - "User visits /verify/{agentId} and sees a Self Protocol QR code they can scan with the Self app"
    - "QR code is configured with the agent operator's wallet address as userId"
    - "Verification page shows clear instructions about downloading Self app and scanning passport"
    - "Verified agents display ZK Verified badge on profile and agent cards (already wired, confirmed working)"
  artifacts:
    - path: "src/components/self/self-qr.tsx"
      provides: "Client component wrapping SelfQRcodeWrapper"
      exports: ["SelfQR"]
    - path: "src/app/verify/[agentId]/page.tsx"
      provides: "Verification page with QR code for specific agent"
      min_lines: 30
  key_links:
    - from: "src/components/self/self-qr.tsx"
      to: "@selfxyz/qrcode"
      via: "SelfAppBuilder + SelfQRcodeWrapper imports"
      pattern: "import.*from.*@selfxyz/qrcode"
    - from: "src/components/self/self-qr.tsx"
      to: "src/lib/chain/self.ts"
      via: "import shared config constants"
      pattern: "import.*SELF_SCOPE.*from.*chain/self"
    - from: "src/app/verify/[agentId]/page.tsx"
      to: "src/components/self/self-qr.tsx"
      via: "renders SelfQR component"
      pattern: "<SelfQR"
    - from: "src/app/verify/[agentId]/page.tsx"
      to: "src/lib/db.ts"
      via: "loads agent data to get wallet_address"
      pattern: "getDb.*agents.*WHERE"
---

<objective>
Create the frontend verification page and QR code component so agent operators can verify their identity via Self Protocol.

Purpose: Complete the user-facing verification flow -- an agent operator visits /verify/{agentId}, sees a QR code, scans it with the Self app and their passport, and the backend (from plan 01) handles the proof validation and marks them verified.

Output: `src/components/self/self-qr.tsx` (QR client component), `src/app/verify/[agentId]/page.tsx` (verification page)
</objective>

<execution_context>
@/Users/konradgnat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konradgnat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-self-protocol-zk/7-RESEARCH.md
@.planning/phases/07-self-protocol-zk/07-01-SUMMARY.md
@src/lib/chain/self.ts
@src/lib/db.ts
@src/lib/types.ts
@src/app/agent/[id]/page.tsx
@src/components/agents/agent-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SelfQR client component</name>
  <files>src/components/self/self-qr.tsx</files>
  <action>
    Create `src/components/self/self-qr.tsx` as a 'use client' component:

    1. Import `SelfAppBuilder` and `SelfQRcodeWrapper` from `@selfxyz/qrcode`
    2. Import `SELF_SCOPE` and `SELF_DISCLOSURES` from `@/lib/chain/self` (shared config prevents mismatch)
    3. Define props interface: `{ userId: string; onSuccess: () => void }`
       - userId is the agent operator's wallet address (hex)
    4. Build selfApp using SelfAppBuilder:
       - appName: 'Network'
       - scope: SELF_SCOPE (from shared config)
       - endpoint: `${window.location.origin}/api/self/verify`
       - endpointType: 'staging_celo' (testnet for hackathon)
       - userId: props.userId
       - userIdType: 'hex'
       - disclosures: SELF_DISCLOSURES (from shared config)
       - Call `.build()` on the builder
    5. Render `<SelfQRcodeWrapper selfApp={selfApp} onSuccess={onSuccess} />` with a dark-themed container div
    6. Style the container to fit the cyberpunk aesthetic -- use existing Tailwind classes from the project (glassmorphism card style with border-cyan-500/20)

    CRITICAL: Use the SHARED config constants from self.ts, NOT hardcoded values. This prevents the config mismatch pitfall documented in research.
  </action>
  <verify>
    - File exists at src/components/self/self-qr.tsx
    - Imports SELF_SCOPE and SELF_DISCLOSURES from chain/self (grep confirms)
    - Exports SelfQR component
    - `pnpm build` succeeds
  </verify>
  <done>
    - SelfQR client component renders SelfQRcodeWrapper with shared config
    - Config values imported from chain/self.ts (not hardcoded)
    - Component accepts userId (wallet) and onSuccess callback
  </done>
</task>

<task type="auto">
  <name>Task 2: Create verification page at /verify/[agentId]</name>
  <files>src/app/verify/[agentId]/page.tsx</files>
  <action>
    Create `src/app/verify/[agentId]/page.tsx` as a server component page that:

    1. Accept params with agentId (follow Next.js 16 async params pattern used in other pages like src/app/agent/[id]/page.tsx)
    2. Load agent from SQLite by id using getDb(): `SELECT * FROM agents WHERE id = ?`
    3. If agent not found, show "Agent not found" message
    4. If agent already has self_verified = 1, show "Already Verified" message with a link back to agent profile
    5. Render the page with:
       - Agent display name and avatar at top
       - Clear instructions section:
         - "1. Download the Self app from App Store or Google Play"
         - "2. Register your passport in the Self app"
         - "3. Scan the QR code below with the Self app"
       - A client wrapper component that renders the SelfQR component (since SelfQR is 'use client')
       - Pass agent.wallet_address as userId to SelfQR
       - onSuccess handler: redirect to `/agent/${agentId}` (use router.push in the client wrapper)
    6. Style with cyberpunk aesthetic matching the rest of the site:
       - Glassmorphism card container
       - Electric cyan accents for headings
       - Grid/noise background (inherited from layout)

    NOTE: The page itself is a server component (loads DB data), but it renders a client component (SelfQR) for the interactive QR code. Create a small client wrapper inline or as part of the page that handles the onSuccess redirect.

    Also add a "Verify Identity" link/button on the agent profile page (src/app/agent/[id]/page.tsx) that links to /verify/{agentId} -- only show this when the agent is NOT yet verified (self_verified === 0). This completes the SELF-03 requirement by ensuring users can discover the verification flow from the profile.
  </action>
  <verify>
    - `pnpm build` succeeds
    - Route /verify/[agentId] is generated in build output
    - Page loads agent from DB and renders SelfQR component
    - Agent profile page has "Verify Identity" link when not yet verified
  </verify>
  <done>
    - /verify/[agentId] page renders QR code for unverified agents
    - Already-verified agents see confirmation message
    - Instructions explain Self app download and passport scanning
    - Agent profile page links to verification page when not yet verified
    - ZK Verified badge displays on profile when self_verified = 1 (already implemented, confirmed working)
  </done>
</task>

</tasks>

<verification>
- src/components/self/self-qr.tsx exists and exports SelfQR
- src/app/verify/[agentId]/page.tsx exists and renders verification flow
- SelfQR imports shared config from chain/self.ts (not hardcoded values)
- Agent profile page has "Verify Identity" link for unverified agents
- `pnpm build` succeeds without TypeScript errors
- Full flow: visit /verify/{agentId} -> see QR -> (scan with Self app) -> agent marked verified -> badge appears on profile
</verification>

<success_criteria>
- Verification page shows QR code scannable by Self app
- Page includes clear user instructions for Self app setup
- Config matches backend exactly (shared constants)
- Already-verified agents see confirmation instead of QR
- Agent profile links to verification page for unverified agents
- ZK Verified badge appears on verified agent profiles and cards
</success_criteria>

<output>
After completion, create `.planning/phases/07-self-protocol-zk/07-02-SUMMARY.md`
</output>
